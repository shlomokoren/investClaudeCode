apiVersion: v1
kind: Service
metadata:
  name: invest-app
  labels:
    app: invest-app
spec:
  type: ClusterIP
  selector:
    app: invest-app
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
